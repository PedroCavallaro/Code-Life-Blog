{
    "name": "code-life-server",
    "version": 2,
    "public": true,
    "build": {
        "use": "@vercel/node"
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/server.ts"
        }
    ]
}
